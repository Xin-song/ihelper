services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-ihelper}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-ihelper}
      POSTGRES_DB: ${POSTGRES_DB:-ihelper}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-ihelper}"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  db_data:
